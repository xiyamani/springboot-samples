### 1、概念

`@Bean` 是 **Spring 框架** 提供的注解，作用是**将方法的返回值对象，注册为 Spring 容器中的 Bean**，交由 Spring 管理生命周期、依赖注入，替代传统 XML 配置文件。

### 2.作用

1. **手动创建并注册 Bean**：适用于第三方类、接口实现类等无法直接用`@Component`标注的类
2. **自定义 Bean 初始化逻辑**：灵活配置对象属性、依赖
3. **控制 Bean 名称、作用域、生命周期**
4. **解耦代码**：统一管理对象创建，不硬编码 new 对象